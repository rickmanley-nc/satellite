---
- name: Check for existing Satellite deployment before continuing
  roles:
    - check-for-existing-satellite

- name: Configure OS (change role names "configure-hostname" "configure-firewall" etc)
  roles:
    - hostname
    - firewall
    - etc-hosts
    - register

- name: Install and Configure Satellite
  roles:
    - install-satellite
    - manifest
    - sync-plan
    - lazy-sync
    - lifecycle-environments
    - product-repo-RHEL7
    - product-repo-EPEL7
    - ccv-RHEL7-EPEL
    - activation-keys
    - provision-libvirt

#check for existing satellite. If it exists, skip only the install portion. Still perform/check the rest?
